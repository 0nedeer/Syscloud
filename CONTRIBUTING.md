# 贡献说明

开始修改前阅读 [功能需求](docs/requirements.md) 和 [架构设计](docs/design.md)。模块职责与阅读顺序见 [源码指南](docs/code-guide.md)，环境参数见 [开发说明](docs/development.md)。

## 代码约定

- Python 3.12，UTF-8 编码，使用 Ruff 统一格式和导入顺序。
- HTTP 路由只处理参数、响应模型和状态码；业务服务管理事务；处理器负责外部调用。
- 为跨模块函数标注参数和返回类型。注释说明事务、并发和恢复约束，避免逐行复述代码。
- 各数据库会话按请求或任务独立创建；不要在数据库事务内等待 ASR 或 LLM。
- 维护“录音 → 任务”的加锁顺序。状态写入通过队列的所有权检查，避免绕过租约。
- 修改表结构时追加 Alembic 迁移；修改公开接口时同步响应模型、OpenAPI 和调用示例。

## 提交检查

在仓库根目录执行：

```text
uv sync --frozen
uv run --frozen ruff check .
uv run --frozen ruff format --check .
git diff --check
git diff --cached
git status --short
```

按具体业务功能组织提交，例如 `feat: 增加录音分页查询`、`fix: 保留摘要重试所需转写`。尽量保持单个提交小而完整，相关代码和文档一起修改。

提交说明应包含修改原因、预期行为、已完成的核查和未覆盖的边界。涉及数据库或模型的改动，要区分静态检查与实际服务验证，不能将两者混为一谈。

## 仓库内容

提交业务代码、迁移、通用工具、依赖锁文件、公开文档及不含真实凭据的配置示例。环境文件、录音、数据库文件、日志、备份、个人路径和开发审阅记录不属于仓库内容。

反馈问题时提供版本、操作步骤和脱敏错误码；不要上传密钥、数据库密码、个人录音或模型原始响应。API 错误中的 request_id 可用于关联本地日志。

## 许可证

当前尚未选择开源许可证，仓库未附加 LICENSE；后续以维护者明确发布的许可证为准。
